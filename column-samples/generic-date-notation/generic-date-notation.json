{
  "$schema": "https://developer.microsoft.com/json-schemas/sp/v2/column-formatting.schema.json",
  "elmType": "div",
  "txtContent": "= getDate([$MeetingMomentStart]) + '-' + getMonth([$MeetingMomentStart]) + '-' + getYear([$MeetingMomentStart]) + ' ' + if(endsWith(toLocaleTimeString([$MeetingMomentStart]),'AM'),if(substring(toLocaleTimeString([$MeetingMomentStart]),1,2)==':',substring(toLocaleTimeString([$MeetingMomentStart]),0,1) + ':' + substring(toLocaleTimeString([$MeetingMomentStart]),2,4),substring(toLocaleTimeString([$MeetingMomentStart]),0,2) + ':' + substring(toLocaleTimeString([$MeetingMomentStart]),3,5)), if(endsWith(toLocaleTimeString([$MeetingMomentStart]),'PM'),if(substring(toLocaleTimeString([$MeetingMomentStart]),1,2)==':',Number(substring(toLocaleTimeString([$MeetingMomentStart]),0,1))+12 + ':' + substring(toLocaleTimeString([$MeetingMomentStart]),2,4),Number(substring(toLocaleTimeString([$MeetingMomentStart]),0,2))+12+ ':' + substring(toLocaleTimeString([$MeetingMomentStart]),2,4)),if(substring(toLocaleTimeString([$MeetingMomentStart]),1,2)==':',substring(toLocaleTimeString([$MeetingMomentStart]),0,1) + ':' + substring(toLocaleTimeString([$MeetingMomentStart]),2,4),substring(toLocaleTimeString([$MeetingMomentStart]),0,2)+ ':' + substring(toLocaleTimeString([$MeetingMomentStart]),3,5))))"
}